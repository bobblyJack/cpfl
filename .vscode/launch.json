{
    "version": "0.2.0",
    "configurations": [
        // debug taskpane using edge
        {
            "type": "msedge",
            "request": "attach",
            "name": "sideload",
            "port": 9229,
            "timeout": 10000,
            "webRoot": "${workspaceRoot}/dist/",
            "preLaunchTask": "Start",
            "postDebugTask": "Stop"
        }
    ]
}
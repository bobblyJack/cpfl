{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "msedge",
            "request": "attach",
            "name": "Debugger",
            "webRoot": "${workspaceFolder}/dist/",
            "port": 9229,
            "preLaunchTask": "Start",
            "postDebugTask": "Stop"
        }
    ]
}
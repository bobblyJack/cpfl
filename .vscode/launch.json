{
    "version": "0.2.0",
    "configurations": [
        // debug taskpane using edge
        {
            "type": "msedge",
            "request": "attach",
            "name": "Debugger",
            "webRoot": "${workspaceFolder}/dist/",
            "port": 9229,
            "preLaunchTask": "Start",
            "postDebugTask": "Stop"
        }
    ]
}
{
    "version": "0.2.0",
    "configurations": [
        // debug taskpane using edge
        {
            "type": "msedge",
            "request": "attach",
            "name": "sideload",
            "useWebView": true,
            "port": 9229,
            "timeout": 600000,
            "webRoot": "${workspaceRoot}/dist/",
            "preLaunchTask": "Start",
            "postDebugTask": "Stop"
        }
    ]
}